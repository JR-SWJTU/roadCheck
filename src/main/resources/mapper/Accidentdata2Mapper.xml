<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.swjtu.roadCheck.mapper.Accidentdata2Mapper" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <select id="getRegionList" resultType="String">
        select distinct Xianqu from accidentdata
    </select>

    <select id="queryAccidentdataByCondition" parameterType="Map" resultType="com.swjtu.roadCheck.entity.Accidentdata">
        SELECT * FROM accidentdata
        <if test="regions != null">
            <where>
                xianQu IN
                <foreach collection="regions" item="region" open="(" separator="," close=")">
                    #{region}
                </foreach>
            </where>
        </if>
        AND accidencnumber IN
        (SELECT shigunumber FROM accidencecollectiondata
        <if test="teams != null">
            <where>
                team_name IN
                <foreach collection="teams" item="team" open="(" separator="," close=")">
                    #{team}
                </foreach>
            </where>
        </if>
        AND environmentnumber IN
        (SELECT emnumber FROM emdata WHERE Jcktype LIKE #{jckType}))
        AND (DATE(riQI) BETWEEN #{startDate} and #{endDate})
    </select>


</mapper>