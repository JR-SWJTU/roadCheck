<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.swjtu.roadCheck.mapper.Accidentdata2Mapper" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <select id="getRegionList" resultType="String">
        select distinct Xianqu from accidentdata
    </select>

    <select id="queryAccidentdataByCondition1" parameterType="Map" resultType="com.swjtu.roadCheck.entity.Accidentdata">
        SELECT * FROM accidentdata
        WHERE xianQu = #{areaName}
        AND accidencnumber IN (
          SELECT shigunumber FROM accidencecollectiondata
          <if test="teamName != null">
            <where>
                team_name IN
                <foreach collection="teamName" item="team" open="(" separator="," close=")">
                    #{team}
                </foreach>
            </where>
          </if>
          AND environmentnumber IN (
            SELECT emnumber FROM emdata
              WHERE Jcktype LIKE "%"#{roadType}"%"
            )
        )
          AND (DATE(riQI) BETWEEN #{startTime} and #{endTime})
    </select>

    <select id="queryAccidentdataByCondition2" parameterType="Map" resultType="com.swjtu.roadCheck.entity.Accidentdata">
        SELECT * FROM accidentdata
        WHERE xianQu = #{areaName}
        AND accidencnumber IN (
        SELECT shigunumber FROM accidencecollectiondata
        <if test="teamName != null">
            <where>
                team_name IN
                <foreach collection="teamName" item="team" open="(" separator="," close=")">
                    #{team}
                </foreach>
            </where>
        </if>
        AND environmentnumber IN (
        SELECT emnumber FROM emdata
        WHERE Jcktype NOT LIKE "%"#{roadType}"%"
        )
        )
        AND (DATE(riQI) BETWEEN #{startTime} and #{endTime})
    </select>

    <select id="queryCrossing" resultType="String">
        SELECT DISTINCT diMingBeiZhu FROM  accidentdata WHERE diMingBeiZhu LIKE '%交叉口'
    </select>

    <select id="quueryOrdinayRoad" resultType="String">
        SELECT DISTINCT diMingBeiZhu FROM  accidentdata WHERE diMingBeiZhu NOT LIKE '%交叉口'
    </select>
</mapper>